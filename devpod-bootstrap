#! /usr/bin/env bash
set -e

[ -f "$HOME/.bashrc" ] && rm "$HOME/.bashrc"
[ -f "$HOME/.profile" ] && rm "$HOME/.profile"

dotfiles=(
  devpod
  nvim
  tmux
  shell
  user_scripts
  tmuxifier
  git
)

. ./setup "${dotfiles[@]}"

mise install

bash -c 'nvim --headless "+Lazy! sync" +qa'
