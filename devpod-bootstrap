#! /usr/bin/env bash

dotfiles=(
  nvim
  tmux
  shell
  user_scripts
  tmuxifier
  git
)

. ./setup "${dotfiles[@]}"

cd "$(dirname $(readlink -f "$0"))/dotfiles"
stow --override=".*" devpod
cd -

mise install
