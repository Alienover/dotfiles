-- Reference
-- https://github.com/simrat39/symbols-outline.nvim

local utils = require("utils")

local g = utils.g

g.symbols_outline = {
  keymaps = {
    close = "q",
    hover_symbol = "<C-k>",
  },
}
